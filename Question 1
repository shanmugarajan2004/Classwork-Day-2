#include <stdio.h>

// Function to find the cube of a given number
int cube(int num) {
   return num*num*num;
}

int main() {
   int num;

   printf("Enter a number: ");
   scanf("%d", &num);

   printf("The cube of %d is %d\n", num, cube(num));

   return 0;
}
